{
  "scripts": {
    "all": "dotenv -e .env concurrently \"anvil\" \"cd contract && dotenv -e ../.env forge script script/Deploy.s.sol --rpc-url $VITE_RPC_URL --broadcast --private-key $PRIVATE_KEY\" \"cd frontend && npm run dev\""
  },
  "dependencies": {
    "concurrently": "^9.1.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv-cli": "^7.4.4"
  }
}
